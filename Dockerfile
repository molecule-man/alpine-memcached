FROM alpine:3.3

ENV MEMCACHED_MEMORY 256
ENV MEMCACHED_MAX_CONNECTIONS 1024

EXPOSE 11211 11211/udp

RUN apk --no-cache add memcached

USER memcached

CMD memcached -v -m $MEMCACHED_MEMORY -p 11211 -c $MEMCACHED_MAX_CONNECTIONS
